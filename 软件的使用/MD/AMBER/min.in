Minimize
 &cntrl
  imin=1,         ! 选择运行能量最小化
  ntx=1,          ! 默认1, 从ASCII格式的inpcrd坐标文件读取坐标, 但不读取速度
  irest=0,        ! 默认0, 不重新启动模拟(不适用于最小化)

  ntpr=500,
    maxcyc=10000, ! 最小化的最大循环数
  ncyc=5000,      ! 最初的0到ncyc循环使用最速下降算法, 此后的ncyc到maxcyc循环切换到共轭梯度算法
  ntwx=0,         ! 不适用于最小化
  cut=8.0,
  saltcon=0.15,
 /
/
